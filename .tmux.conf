setw -g mode-keys vi
set-option -g focus-events on

set-option -sa terminal-overrides ",xterm*:Tc"

############# HOTKEYS ############
unbind C-b

set-option -g prefix C-a
bind-key C-a send-prefix

unbind |
bind \\ split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

bind -n M-F11 prev
bind -n M-F12 next
bind -n M-F8 kill-window

bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D
bind -n M-d kill-pane

bind -n C-F11 swap-window -t -1\; select-window -t -1
bind -n C-F12 swap-window -t +1\; select-window -t +1
bind -n M-k next
bind -n M-j prev

bind -n M-r source-file ~/.tmux.conf
set -sg escape-time 0

#bind -n M-Enter new-window -a -c ~/
bind -n M-Enter run-shell "~/.scripts/tmux/window_new.sh"
bind Enter run-shell "~/.scripts/tmux/window_popup.sh > /dev/null || true"
bind r run-shell "~/.scripts/tmux/window_rename.sh"
bind -n M-F4 run-shell "~/.scripts/tmux/session_switch_main.sh"
bind a run-shell "~/.scripts/tmux/session_new.sh"
bind s run-shell "~/.scripts/tmux/session_jump.sh"

set-hook -g window-unlinked 'run "~/.scripts/tmux/session_close_hook.sh"'
set-hook -g session-closed 'run "~/.scripts/tmux/session_switch_main.sh on_close"'

#unbind-key Escape

##################################

set -g mouse on
set -g history-limit 1000
set-option -g allow-rename off
set -g default-terminal "tmux-256color"
#set -g default-terminal "xterm-256color"
set-option -g xterm-keys on
set-option -g detach-on-destroy off

#######################
#        THEME        #
#######################

## COLORSCHEME: gruvbox dark (medium)
set-option -g status "on"

# default statusbar color
#set-option -g status-style bg=colour237,fg=colour223 # bg=bg1, fg=fg1
set-option -g status-style bg='#272e33',fg='#d3c6aa' # bg=bg1, fg=fg1

# default window title colors
set-window-option -g window-status-style bg=colour214,fg=colour237 # bg=yellow, fg=bg1

# default window with an activity alert
set-window-option -g window-status-activity-style bg=colour237,fg=colour248 # bg=bg1, fg=fg3

# active window title colors
set-window-option -g window-status-current-style bg=red,fg=colour237 # fg=bg1

# pane border
set-option -g pane-active-border-style fg=colour241 #fg2
set-option -g pane-border-style fg=colour238 #bg1

# message infos
set-option -g message-style bg=colour239,fg=colour223 # bg=bg2, fg=fg1

# writing commands inactive
set-option -g message-command-style bg=colour239,fg=colour223 # bg=fg3, fg=bg1

# pane number display
set-option -g display-panes-active-colour colour250 #fg2
set-option -g display-panes-colour colour237 #bg1

# clock
set-window-option -g clock-mode-colour colour109 #blue

# bell
set-window-option -g window-status-bell-style bg=colour167,fg=colour235 # bg=red, fg=bg

set -g status-interval 1

## Theme settings mixed with colors (unfortunately, but there is no cleaner way)
set-option -g status-justify "left"
set-option -g status-left-style none
set-option -g status-left-length "80"
set-option -g status-right-style none
set-option -g status-right-length "80"
set-window-option -g window-status-separator ""

set-option -g status-left "#[bg=colour241,fg=colour248]#[bg=colour237,fg=colour241,nobold,noitalics,nounderscore]"
set-option -g status-right "#[bg=#272e33,fg=colour246]#(~/.scripts/tmux/git_status.sh #{pane_current_path}) #[bg=#272e33,fg=colour248,nobold,noitalics,nounderscore]   #S "

set-window-option -g window-status-current-format "#[bg=colour240,fg=#d3c6aa] #I:#[bg=colour240,fg=#d3c6aa] #W#{?window_zoomed_flag,*Z,} "
set-window-option -g window-status-format "#[bg=#272e33,fg=#d3c6aa] #I:#[bg=#272e33,fg=#d3c6aa] #W "

#run-shell ~/.tmux/tmux-yank/yank.tmux

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'sainnhe/tmux-fzf'

run '~/.tmux/plugins/tpm/tpm'
